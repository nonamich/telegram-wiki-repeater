{
  "name": "telegram-wiki",
  "private": true,
  "packageManager": "npm@10.2.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bot": "turbo run --filter=bot...",
    "bot:build": "npm run bot -- build",
    "bot:dev": "npm run bot -- --parallel dev"
  },
  "devDependencies": {
    "turbo": "^2.0.4"
  }
}

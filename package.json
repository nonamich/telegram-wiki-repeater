{
  "name": "telegram-wiki",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "bot": "turbo run --filter=bot...",
    "bot:build": "npm run bot -- --concurrency=1 build",
    "bot:dev": "npm run bot -- --parallel dev"
  },
  "devDependencies": {
    "turbo": "^1.11.3"
  }
}
